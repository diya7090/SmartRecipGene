@model IEnumerable<SmartRecipGene.Models.FavoriteRecipe>

@{
    ViewData["Title"] = "My Favorite Recipes";
}

<h2>My Favorite Recipes</h2>

@if (!Model.Any())
{
    <p>You have no favorite recipes yet.</p>
}
else
{
    <div class="row">
        @foreach (var recipe in Model)
        {
            <div class="col-md-4">
                <div class="card">
                    <img src="@recipe.ImageUrl" class="card-img-top" alt="@recipe.Title">
                    <div class="card-body">
                        <h5 class="card-title">@recipe.Title</h5>
                        <a href="/Home/RecipeDetails?id=@recipe.RecipeId" class="btn btn-primary">View Recipe</a>

                        <form method="post" asp-action="RemoveFromFavorites">
                            <input type="hidden" name="id" value="@recipe.Id" />
                            <button type="submit" class="btn btn-danger">❌ Remove</button>
                        </form>
                    </div>
                </div>
            </div>
        }
    </div>
}
