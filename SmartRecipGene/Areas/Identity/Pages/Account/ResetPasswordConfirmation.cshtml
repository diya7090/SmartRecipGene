@* @page
@model ResetPasswordConfirmationModel
@{
    ViewData["Title"] = "Reset password confirmation";
}

<h1>@ViewData["Title"]</h1>
<p>
    Your password has been reset. Please <a asp-page="./Login">click here to log in</a>.
</p>
<style>
    .message-box {
        max-width: 600px;
        margin: 5rem auto;
        padding: 2.5rem;
        background: #fff;
        border-radius: 12px;
        box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
        text-align: center;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

        .message-box h1 {
            font-size: 1.9rem;
            color: #e67e22;
            font-weight: 600;
            margin-bottom: 1rem;
        }

        .message-box p {
            font-size: 1.15rem;
            color: #333;
        }

        .message-box a {
            color: #e67e22;
            font-weight: 500;
            text-decoration: underline;
        }

            .message-box a:hover {
                color: #cf711f;
            }
</style> *@

        @page
@model ResetPasswordConfirmationModel
@{
    ViewData["Title"] = "Reset password confirmation";
}

<div class="container message-box animate-fadein">
    <h1><i class="bi bi-unlock-fill text-warning me-2"></i> @ViewData["Title"]</h1>
    <p>
        🔑 Your password has been reset. Please
        <a asp-page="./Login" class="login-link">click here to log in</a>.
    </p>
</div>

<style>
    .message-box {
        max-width: 600px;
        margin: 5rem auto;
        padding: 2.5rem;
        background: #fff;
        border-radius: 12px;
        box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
        text-align: center;
        animation: fadeInUp 1s ease;
    }

    .message-box h1 {
        font-size: 1.9rem;
        color: #e67e22;
        font-weight: 600;
        margin-bottom: 1rem;
    }

    .message-box p {
        font-size: 1.15rem;
        color: #333;
    }

    .login-link {
        color: #e67e22;
        font-weight: 500;
        text-decoration: none;
        transition: all 0.3s ease;
    }

    .login-link:hover {
        color: #cf711f;
        text-decoration: underline;
    }

    @@keyframes fadeInUp {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
    }
</style>
